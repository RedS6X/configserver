server:
port: 8888

spring:
   cloud:
      config:
         server:
            git:
               uri: https://github.com/RedS6X/configserver.git
               default-label: main
               search-paths:
               - '*service'